#from modeling import Metalearning
#from data.dataloader import build_train_loader_for_m_resnet
#from torch import nn

from torchvision.transforms import Resize
pil_interp_from_str = {
        'nearest': 1,
        'bilinear': 2,
        'bicubic': 3,
        'box': 4,
        'lanczos': 5,
        'hamming': 6
    }[3]
#train_loader, mtrain_loader, mtest_loader, num_domains = build_train_loader_for_m_resnet(batch_size=32)
#for x in train_loader:
#    break
print()

